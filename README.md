# Messenger

## Getting Started

Install dependencies:

```bash
npm install
```

Start db:

```bash
docker compose up db
```

Run dev server:

```bash
npm run dev
```

All in docker:
'''bash
docker compose up
''''

Open in browser: <http://localhost:3000>
